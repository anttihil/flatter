import React from "react";

const PostList = (params) => {
  /* take a category as a param
    if no param, then fetch and render all posts
    State that consists of posts
    fetch posts from server on render
    useParams from the route/location
    use route to establish numbered sub-pages of 10 posts /pokemon/1, /pokemon/2, pokemon/3 
    sorting function should be at the top
    sorting function should be a context or then i have to lift the state, or finally, i have to put the sorting element at the top level
    pass the sorting choice as props to postlist as a param


     */
};

export default PostList;
