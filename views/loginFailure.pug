extends layout 

block content 
    div.flex.flex-col.items-center.justify-center.bg-main.h-full
        .bg-dark.p-4.flex.flex-col.items-center.w-96.divide-y-2.space-y-2.divide-brown.text-light 
            div.flex.flex-col.items-center
                header.font-fraktur.text-xl Login failed
                if hasMessages
                    each message in messages 
                        article.font-serif= message 
            a(href= "/login") Try again.